# 脚本调试说明(SKS Debug)

Maximo 自动化脚本(JavaScript/Nashorn、Jython)远程调试功能,调试器运行在 Maximo JVM 内,
VS Code 通过 TCP(DAP 协议)直接连接,不需要 JDWP。

> 配套脚本测试样例:`masscript\ibm\test\TEST_GRAALVM_API01.js`(视项目而定)

---

## 一、工作原理

```
VS Code (maximo-script-helper attach)
   │ 1. REST: 检查 SKS.AUTOSCRIPT.DEBUG 脚本是否存在(不存在会提示)
   │ 2. REST: 检查/上传驱动 jar(sks-autoscript-debug.jar)并激活驱动
   │ 3. TCP   : 连接脚本调试端口(DAP)
   ▼
Maximo JVM
   ├─ SKS.AUTOSCRIPT.DEBUG      (Maximo 自动化脚本,负责 jar 安装/驱动加载/卸载)
   ├─ DebugDriver               (cn.shoukaiseki.autoscript.DebugDriver,热加载)
   └─ DebugAdapterServer        (监听调试端口)
        ├─ JavaScript/Nashorn: 源码插桩断点/单步/变量快照
        └─ Jython            : trace hook
```

- JS 断点靠**源码插桩**,不受 Nashorn 隐藏类限制。
- 断点按**文件名**映射(本地文件名大写 = Maximo 脚本名)。
- 断开调试时自动卸载驱动并恢复脚本引擎表。

---

## 二、服务器端准备

在 Maximo **系统属性(System Properties)** 应用中创建以下属性(描述必填,注意数据类型):

| 属性名 | 描述(必填) | 数据类型 | 值 |
|---|---|---|---|
| `sks.autoscript.debug.enabled` | SKS脚本调试总开关:1=启用Maximo内嵌调试适配器,0=关闭;修改后需重启Maximo生效 | YORN | 1 |
| `sks.autoscript.debug.port` | SKS脚本调试端口:Maximo内嵌调试适配器监听端口,须与VS Code连接配置里的"脚本调试端口"一致,默认9229;修改后需重启Maximo生效 | INTEGER | 9229 |
| `sks.autoscript.debug.host` | SKS脚本调试监听地址:0.0.0.0=所有网卡(容器/远程可连),127.0.0.1=仅本机;修改后需重启Maximo生效 | ALN | 0.0.0.0 |

等价配置文件写法(也可直接放入 maximo.properties 或 JVM 参数):

```properties
sks.autoscript.debug.enabled=1
sks.autoscript.debug.port=9229
sks.autoscript.debug.host=0.0.0.0
```

- `enabled=1` 是总开关,不配则端口永远不会监听。
- `host` 必须 `0.0.0.0`(容器外可连)。
- **调试端口必须与 VS Code 连接配置里的"脚本调试端口"一致**,否则连不上。

可选属性:

| 属性名 | 描述(必填) | 数据类型 | 默认值 |
|---|---|---|---|
| `sks.autoscript.debug.js.exclude` | 需要排除插桩的JS脚本名列表(逗号分隔,大小写不敏感);为空时使用内置默认排除列表 | ALN | 空 |
| `sks.autoscript.breakOnException` | 是否在脚本捕获到异常时暂停(需调试器已附加) | YORN | 0 |
| `sks.autoscript.breakOnUncaught` | 是否在脚本未捕获异常时暂停 | YORN | 0 |
| `sks.autoscript.debug.client.idleTimeout` | 调试客户端闲置超时(毫秒),0=不超时 | INTEGER | 0 |
| `sks.autoscript.debug.client.poll` | 调试客户端活跃心跳轮询间隔(毫秒) | INTEGER | 1000 |

容器端口映射(docker-compose):

```yaml
ports:
  - "9229:9229"
```

### OCP(OpenShift)环境

Maximo 运行在 OCP 时,需要把调试端口从容器暴露出来供 VS Code 连接。端口分三个层级,别搞混:

| 层级 | 端口 | 说明 |
|---|---|---|
| 容器内监听 targetPort | `<容器端口>`(如 4711) | 必须与 `sks.autoscript.debug.port` 一致 |
| Service 端口 | `<服务端口>`(如 4711) | `oc port-forward svc/<服务名>` 时使用的远端端口 |
| NodePort 集群对外端口 | `<NodePort端口>`(如 30471,范围 30000-32767) | 集群节点上暴露,外部能否直连取决于网络/防火墙/VPN |

1) 给应用新增 NodePort 服务(名称/标签/端口用占位符替换为实际值):

```yaml
apiVersion: v1
kind: Service
metadata:
  name: <调试服务名>
  namespace: <命名空间>
spec:
  type: NodePort
  selector:
    <标签名>: <标签值>
  ports:
    - name: autoscript-debug
      port: <服务端口>        # 如 4711
      targetPort: <容器端口>  # 如 4711,与 sks.autoscript.debug.port 一致
      nodePort: <NodePort端口> # 如 30471
```

2) 确认服务与 pod 标签匹配:

```bash
oc project <命名空间>
oc get svc <调试服务名> -o wide
```

3) 确认 Maximo 属性:`sks.autoscript.debug.enabled=1`、`sks.autoscript.debug.port=<容器端口>`、`sks.autoscript.debug.host=0.0.0.0`;
   驱动激活成功后 pod 内应有监听:

```bash
oc exec <pod名> -- bash -c "ss -tlnp | grep <容器端口>"
```

> 若转发时报 `connection refused inside namespace`,表示 pod 内驱动未激活/端口没监听,而非转发配置问题。

4) 本地端口转发(最常用,不依赖 NodePort 对外可达):

```bash
oc port-forward -n <命名空间> svc/<调试服务名> <本地端口>:<服务端口>
```

例如本机监听 `127.0.0.1:<本地端口>`,转发到集群的服务端口;成功后 oc 会打印
`Forwarding from 127.0.0.1:<本地端口> -> <容器端口>`。
具体显示以 `oc get svc` 输出的端口为准(服务端口与容器端口未必相同)。

权限要求(与其它 REST 操作一致):
- 连接账号为 maxadmin,或授予 `MXSCRIPT`/`MXAPIDOMAIN`(底层 MAXDOMAIN)访问权限。

---

## 三、调试脚本与驱动 jar

- **SKS.AUTOSCRIPT.DEBUG**(Maximo 侧脚本):统一由
  `public\maximo-developer-resources\SKS.AUTOSCRIPT.DEBUG.js` 管理,
  用项目自己的部署流程发布到 Maximo(脚本语言 javascript)。
  插件 attach 时**只检查脚本是否存在**,不存在会弹窗提示部署路径,不会自动创建。
- **驱动 jar**:插件内置 `resources\sks-autoscript-debug.jar`
  (包名 `cn.shoukaiseki.autoscript`,类 `DebugDriver`),attach 时按需上传到
  服务器 `java.io.tmpdir`(默认 `/tmp/sks-autoscript-debug.jar`)并激活。

---

## 四、连接配置

- 打开 **Maximo Script Helper 配置** → 连接配置,最下方 **🔧 脚本调试 (Debug)** 分区:
  - **脚本调试端口**:默认 `9229`,保存环境时随环境一起存入 envs.json,切换环境自动切换;
  - **调试主机名(可选)**:留空按服务器地址自动解析;OCP/SSH 端口转发场景填 `127.0.0.1`;
  - 与服务器 `sks.autoscript.debug.port` 一致(容器内端口)。
- **两种连法**(OCP 场景详见"二、服务器端准备 → OCP(OpenShift)环境"):
  - **端口转发**:`oc port-forward -n <命名空间> svc/<服务名> <本地端口>:<服务端口>` 后,
    调试主机名填 `127.0.0.1`、脚本调试端口填 `<本地端口>`;
  - **NodePort 直连**:NodePort 对外可达时,调试主机名填节点地址、脚本调试端口填 `<NodePort端口>`。
- 服务器属性 `sks.autoscript.debug.port` 始终是**容器内端口**(如 `4711`),与客户端填的端口无关。
- 脚本目录:读取"其它配置 → 脚本存放目录(scriptStoragePath,默认 masscript)",
  launch.json 无需再写 scriptRoots(写了则优先)。

---

## 五、使用步骤

1. launch.json 添加(可省略 scriptRoots):

```json
{
  "type": "maximo-script-helper",
  "request": "attach",
  "name": "附加到Maximo自动化脚本",
  "configurations": [
    {
      "type": "maximo-script-helper",
      "request": "attach",
      "name": "附加到Maximo自动化脚本"
    }
  ]
}
```

2. VS Code 运行该 attach 配置(F5),首次会自动检查脚本、上传/激活驱动;
3. 在目标 .js/.py 文件断点;
4. 触发脚本(如 REST `POST /maximo/oslc/script/<脚本名>`、页面操作等);
5. 常用操作:
   - F10 单步跳过、F11 进入函数、Shift+F11 跳出;
   - VARIABLES 查看变量;条件断点用 JS 语法(`===`);
   - 手动断点:

     ```javascript
     if (typeof __autoscript_debugger !== "undefined" && __autoscript_debugger.isEnabled()) {
         __autoscript_debugger.breakpoint("手动断点演示");
     }
     ```

   - 脚本 `print(...)` 输出会转发到调试控制台。

6. 调试完直接断开,驱动自动卸载,无需重启服务器。

---

## 六、日志与排障

- 输出频道 **SKS Debug**:attach 全流程日志(脚本状态/驱动安装/连接)。
- HTTP 日志:配置里开启 "启用 HTTP 请求日志"(enableHttpLog)后,请求/响应会保存为
  IntelliJ HTTP Client 格式的 `.http` 文件(在系统临时目录 `maximo-script-helper` 下)。

常见问题:

| 现象 | 原因 | 处理 |
|---|---|---|
| 提示"服务器上不存在自动化脚本 SKS.AUTOSCRIPT.DEBUG" | 脚本未部署 | 部署 `public\maximo-developer-resources\SKS.AUTOSCRIPT.DEBUG.js` 到 Maximo |
| `BMXAA0024E READ not allowed on MAXDOMAIN` | 连接账号缺权限 | 用 maxadmin 或补 MXAPIDOMAIN/MAXDOMAIN 授权 |
| `connect ECONNREFUSED :端口` | 驱动未激活/端口未映射/enabled 不是 1 | 按第二节核对,看 SKS Debug 日志 |
| 断点不命中 | 本地文件名≠脚本名 / 触发时没 attach / 脚本被 js.exclude | 核对名称,先 attach 再触发 |
| 行为还是旧代码 | Maximo 脚本缓存 | 刷新脚本缓存或重启 |

---

## 七、调试测试脚本(TEST_GRAALVM_API01.js)

本地 `TEST_GRAALVM_API01.js` 内含调试练习代码(变量/循环/函数/Java.type/try-catch/
手动断点),注释中标注了【断点】便于直接打断点。修改后记得**重新部署到 Maximo**。